<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="LaunchTheme" parent="@android:style/Theme.Black.NoTitleBar">
        <!-- Show a splash screen on the activity. Automatically removed when
             Flutter draws its first frame -->
        <item name="android:windowSplashScreenBackground">@color/splashScreenBackground</item>
        <item name="android:windowSplashScreenAnimatedIcon">@drawable/logo_dark</item>
        <item name="android:windowSplashScreenBrandingImage">@drawable/by_niaefeup</item>
    </style>
</resources>